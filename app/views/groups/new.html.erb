<div class="min-h-screen bg-base-200 p-4">
  <%= render 'shared/error_messages', object: @group%>
  <div class="max-w-md mx-auto">
    <!-- フォームカード -->
    <div class="bg-base-100 p-6 rounded-b-lg shadow-lg">
      <%= form_with model: @group, data: { turbo: false } do |f| %>
        
        <!-- グループタイトル -->
        <div class="form-control mb-6">
          <%= render 'shared/form_field', label_text: "グループ名", required: true do%>
            <%= f.text_field :name, 
                placeholder: "いつものメンバー",
                class: "input input-bordered w-full bg-base-200" %>
          <% end %>
        </div>

        <!-- 送信ボタン -->
        <div class="form-control">
          <%= f.submit "リンクを作成", 
              class: "btn btn-primary w-full text-white text-lg rounded-full normal-case hover:pink-custom-hover border-0" %>
        </div>

      <% end %>
    </div>
  </div>
</div>